import Layout from '../components/Layout';

const Error = props => {
    return (
        <Layout>
            <div>
                <h3>Oops... looks like there was an error.</h3>
            </div>
        </Layout>
    );
};

export default Error;
